# DESCRIPTION — Independent ingestion pipelines

A standard-library Python package with three independent ingestion pipelines for typed
CSV batches, JSON Lines events, and shell-style key-value records.
