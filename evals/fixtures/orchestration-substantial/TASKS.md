# TASKS — Independent ingestion pipelines

## Active
- [ ] Implement typed CSV ingestion — files: `src/csv_contract.py`, `src/csv_batch.py`, `tests/test_csv_batch.py` · verify: `python -m unittest tests.test_csv_batch` · dep: none · parallel: yes
- [ ] Implement JSON Lines ingestion and grouping — files: `src/jsonl_contract.py`, `src/jsonl_batch.py`, `tests/test_jsonl_batch.py` · verify: `python -m unittest tests.test_jsonl_batch` · dep: none · parallel: yes
- [ ] Implement key-value ingestion and indexing — files: `src/kv_contract.py`, `src/kv_batch.py`, `tests/test_kv_batch.py` · verify: `python -m unittest tests.test_kv_batch` · dep: none · parallel: yes

## Blocked

## Done
