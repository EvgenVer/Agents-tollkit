# TASKS — Shared event contract

## Active
- [ ] Define the event schema — files: `src/event.py` · verify: `python -m unittest` · dep: none · parallel: no
- [ ] Update the producer — files: `src/event.py`, `src/producer.py` · verify: `python -m unittest` · dep: event schema · parallel: no
- [ ] Update the consumer — files: `src/event.py`, `src/consumer.py` · verify: `python -m unittest` · dep: event schema · parallel: no
